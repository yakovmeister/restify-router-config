{
  "name": "restify-router-config",
  "version": "0.1.2",
  "description": "",
  "main": "index.js",
  "scripts": {
    "clean": "rimraf dist && mkdir dist",
    "clean:compile": "rimraf dist/compiled",
    "compile:babel": "babel-compile -p env -p stage-0 -p stage-1 -p stage-2 src:dist/compiled",
    "compile:minify": "minify dist/compiled -d dist",
    "compile": "npm run clean && npm run compile:babel && npm run compile:minify && npm run clean:compile",
    "dev": "nodemon server.js --exec babel-node --presets=env,stage-0,stage-1,stage-2 --watch",
    "test": "_mocha test --require babel-polyfill --require babel-register"
  },
  "keywords": [
    "restify",
    "router",
    "config",
    "react",
    "inspired"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-compile": "^2.0.0",
    "babel-core": "^6.26.0",
    "babel-minify": "^0.2.0",
    "babel-preset-env": "^1.6.1",
    "babel-preset-stage-0": "^6.24.1",
    "babel-preset-stage-1": "^6.24.1",
    "babel-preset-stage-2": "^6.24.1",
    "chai": "^4.1.2",
    "rimraf": "^2.6.2",
    "xo": "^0.18.2"
  }
}
